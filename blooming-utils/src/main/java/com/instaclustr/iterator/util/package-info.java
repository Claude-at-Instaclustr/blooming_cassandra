package com.instaclustr.iterator.util;
/**


A package for defining useful iterators and iterator operations,
including concatenation, mapping, filtering, empty and singleton
iterators, iterator wrappers, and the <code>ExtendedIterator</code>
class.

Based on the Apache Jena package: @{code org.apache.jena.util.iterator} this
code allows closing iterators to throw an exception.

 */